use clap::Args;

/// Verify a signature on the envelope using the provided public key base.
#[derive(Debug, Args)]
pub struct VerifyArgs {
}

pub fn verify_command(args: &VerifyArgs) {
    todo!();
}
